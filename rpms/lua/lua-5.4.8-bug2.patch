diff -up lua-5.4.8/src/lua.c.54bug2 lua-5.4.8/src/lua.c
--- lua-5.4.8/src/lua.c.54bug2	2026-01-14 16:39:49.725549980 -0500
+++ lua-5.4.8/src/lua.c	2026-01-14 16:41:54.991449713 -0500
@@ -302,7 +302,8 @@ static int collectargs (char **argv, int
       case '-':  /* '--' */
         if (argv[i][2] != '\0')  /* extra characters after '--'? */
           return has_error;  /* invalid option */
-        *first = i + 1;
+        /* if there is a script name, it comes after '--' */
+        *first = (argv[i + 1] != NULL) ? i + 1 : 0;
         return args;
       case '\0':  /* '-' */
         return args;  /* script "name" is '-' */
