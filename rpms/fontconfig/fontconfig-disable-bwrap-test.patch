diff -pruN fontconfig-2.17.0.orig/test/run-test.sh fontconfig-2.17.0/test/run-test.sh
--- fontconfig-2.17.0.orig/test/run-test.sh	2025-01-11 12:00:00.000000000 +0000
+++ fontconfig-2.17.0/test/run-test.sh	2025-01-11 12:00:00.000000000 +0000
@@ -238,7 +238,9 @@ if [ "x$EXEEXT" = "x" ]; then
     exit 1
 fi
 
-if [ x"$BWRAP" != "x" ] && [ "x$EXEEXT" = "x" ]; then
+# Disable bwrap test - it requires user namespaces which are not available
+# in container build environments (mock/podman)
+if false && [ x"$BWRAP" != "x" ] && [ "x$EXEEXT" = "x" ]; then
 dotest "Basic functionality with the bind-mounted cache dir"
 prep
 cp "$FONT1" "$FONT2" "$FONTDIR"
