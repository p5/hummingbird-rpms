---
# skip annocheck unicode test
# test fails for kubectl and kubelet on this symbol which looks correct:
# 'go:(*struct { io.Reader; io.WriterTo }).Read'
 annocheck:
    extra_opts:
        - hardened: --skip-unicode
...
