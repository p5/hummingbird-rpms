{%- for rpm in rpms %}
# --- {{ rpm.name }} ---
{%- if rpm.imported %}
{{ component(rpm) }}
{{ image_repository(rpm) }}
{%- else %}
# PENDING
{%- endif %}
{%- endfor %}
{{ policy("registry-hummingbird") }}
